Installation
------------
1) Download files
2) Extract if necessary
3) Compile
	javac *
		If the compiler complains objects cannot be found use
		javac MPGEntry; javac MPGStore; javac MPGCalc;
4) Run
	java MPGCalc
